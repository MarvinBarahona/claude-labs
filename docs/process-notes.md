# Process Notes

Append-only log of suggestions the workflow can't apply itself: coding-convention/process changes, and changes to files it doesn't own (`README.md`, `CLAUDE.md`, other skills). Written by `plan-work-item` and `graduate-work-item`, never applied by either. Reviewed and cleared manually, on your own schedule.

- **`frontend-browser-e2e-tests`** — graduated (see `docs/shared/frontend-browser-e2e-tests.md`). ~~(1) `testing-strategy.md`'s new "Frontend browser E2E" bucket~~ and ~~(2) `repo-layout.md`'s new top-level `e2e/` directory~~ were both applied directly at graduation, per `graduate-work-item`'s handling of technical decisions. Still outstanding: (3) a new `.claude/skills` entry, parallel to `browser-preview-check`, documenting the exact `docker compose -f docker-compose.dev.yml run --rm e2e` command for running the suite later — no `*-work-item` skill can create a skill file itself.
- **`frontend-browser-e2e-tests`** — `structured-output-console.html`'s result div picked up a `data-testid="structured-result"` during that task's build, mirroring `messages-console`'s existing `data-testid="transcript-list"` convention, so a browser-driven test could scope its assertions past the inspector panel's own JSON dump (which can coincidentally contain the same rendered text). Worth keeping in mind as a precedent — a lab's own demo-result container getting a `data-testid` for exactly this scoping reason — if a future lab's browser-E2E spec runs into the same ambiguity.

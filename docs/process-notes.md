# Process Notes

Append-only log of suggestions the workflow can't apply itself: coding-convention/process changes, and changes to files it doesn't own (`README.md`, `CLAUDE.md`, other skills). Written by `plan-work-item` and `graduate-work-item`, never applied by either. Reviewed and cleared manually, on your own schedule.

- `fake-mode`: once built, add a line to `CLAUDE.md`'s "Running the app" section noting `FAKE_MODE=true` in `backend/.env` as the way to run the app for development/manual exploration without real credentials — otherwise this option is invisible to a session that only reads `CLAUDE.md`/`README.md` automatically.
- `fake-mode`: once the task is complete and the pattern is proven, record in `testing-strategy.md` that no feature needs its own "test under both `FAKE_MODE=true` and `FAKE_MODE=false`" matrix. A feature's business logic is already fully covered by the existing test buckets (backend unit tests inject a fake client directly into the test module; integration tests use `nock`), neither of which goes through the `FAKE_MODE` env var — that flag only matters for the live `docker compose up` process. A feature's own `useFactory: config.fakeMode ? fake : real` wiring is trivial pass-through, already covered by "skip tests for... framework wiring with no logic of its own," and trusting it means trusting the shared DI-switch helper, which fake-mode's own test suite proves once, generically. The real-mode path (a real client actually reaching the real API) stays manually verified either way, same as today — no test ever holds a real credential.
- `prod-docker`: once built, add a line to `CLAUDE.md`'s "Running the app" section documenting `docker compose -f docker-compose.prod.yml up --build` as the prod/deployment run command, alongside the existing dev commands — otherwise this option is invisible to a session that only reads `CLAUDE.md`/`README.md` automatically.

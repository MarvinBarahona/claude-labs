# Process Notes

Append-only log of suggestions the workflow can't apply itself: coding-convention/process changes, and changes to files it doesn't own (`README.md`, `CLAUDE.md`, other skills). Written by `plan-work-item` and `graduate-work-item`, never applied by either. Reviewed and cleared manually, on your own schedule.

- **`foundations-console`** — this feature was the first to need request-body validation, and added `class-validator`/`class-transformer` plus a global `ValidationPipe` (`whitelist: true, transform: true` in `main.ts`) to do it. Worth folding into `nest-conventions` as the standing DTO-validation convention now that a precedent exists, rather than leaving each future feature to rediscover it.
- **`foundations-console`** — several first-pass comments written during this build ran multiple lines explaining rationale that either was already obvious from the code or belonged in the work item's own plan/permanent doc instead. Worth a one-line reminder in `nest-conventions`/`angular-conventions` (or wherever repo-wide code style already lives) that comments should stay to one short line, with a long WHY going in docs rather than the source file.
- **`foundations-console`** — the frontend-integration test bucket (`testing-strategy.md`) has no implemented example yet. Foundations Console was the first candidate — a real backend process, fake `AnthropicClient` bound, driven over real HTTP including SSE — but no precedent existed anywhere under `frontend/` for standing up that backend process from a frontend test, so its "real round trip" scenarios were instead verified manually (Playwright, against the real dev stack in fake mode) rather than automated. Flagged here instead of updated directly in `testing-strategy.md`, at the user's request, for a closer look before it's folded in — a future feature that also streams (e.g. Live Tool-Use Console) is the next natural candidate to establish the actual pattern.
